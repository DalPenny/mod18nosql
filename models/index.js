const User = require('./User');           // user model
const Thought = require('./Thought');     // thought model
// const Reaction = require('./Reaction');   // reaction model

// module.exports = { User, Thought, Reaction };

module.exports = { User, Thought };